node_modules/
# dist/ IS committed — git-tag consumers install it directly (no build-on-install).
# When the kit moves to a registry, restore this ignore and build dist in CI.
*.tsbuildinfo
.DS_Store
